{{- if eq .Values.clusterrole.enabled true -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "registration.serviceAccountName" . }}
rules:
{{- with .Values.clusterrole.rules }}
  {{- toYaml . | nindent 2 }}
{{- end -}}
{{- end }}
